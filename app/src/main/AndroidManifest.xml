<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buscadordehoteis">

    <uses-permission android:name="android.permission.INTERNET">

    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuscadorDeHotéis">
        <activity android:name=".view.TelaCadastro"/>
        <activity android:name=".view.TelaAlterarCadastro"/>
        <activity android:name=".view.TelaPrincipal" />
        <activity android:name=".view.TelaLogin"/>
        <activity android:name=".view.TelaSplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>